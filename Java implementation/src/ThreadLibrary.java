public abstract class ThreadLibrary {

	/**
	 * WHAT?!?!
	 * el thread library deberia tener algo como un next
	 * que me diga cual es la proxima que debo correr,
	 * o sea, deberia tener algo como un getNext()
	 * y que este se encargue de decirme, segun el algoritmo
	 * si cambio o no de Thread que estan guardados en el Process.
	 * Esto no te lo cambio (te lo dejo así) para que lo 
	 * charlemos en algun momento
	 */
    public abstract void addThread(Thread thread);
}
